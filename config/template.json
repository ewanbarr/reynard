{
    "nodes": [
    {% for node in nodes%}
    {
        "name": "{{node.name}}",
        "pipelines":
        [
            {
                "pipeline_name":"TestPipeline",
                "config":
                {
                    "config_path":"/Users/ebarr/Soft/MeerKAT/MGMT/reynard/scripts/pipelines",
                    "sensor_server_ip":"{{sensor_server_ip}}",
                    "sensor_server_port":"{{sensor_server_port}}"
                }
            },
            {
                "pipeline_name":"TestPipeline",
                "config":
                {
                    "config_path":"/Users/ebarr/Soft/MeerKAT/MGMT/reynard/scripts/pipelines",
                    "sensor_server_ip":"{{sensor_server_ip}}",
                    "sensor_server_port":"{{sensor_server_port}}"
                }
            }
        ]
    }
    {% if not loop.last %},{% endif %}
    {% endfor %}
    ]
}