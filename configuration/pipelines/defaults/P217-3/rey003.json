{
    "backend":"effpaf",
    "nodes": [
    {% for node in nodes%}
    {
        "host": "{{node['host']}}",
        "port": {{node['port']}},
        "pipelines":
        [
            {
                "name":"pipeline-0",
                "pipeline_name":"PafFrbPipeline",
                "config":
                {
                    "image":"docker.mpifr-bonn.mpg.de:5000/paf-pipeline:latest",
                    "output_path":"/media/scratch/data/paf/filterbanks",
                    "config_file_params":
                    [
                        "IP {{node['nics'][0]}}",
                        "GPU_ID 0",
                        "SOURCE dummysource",
                        "TELESCOPE_ID 8",
                        "MACHINE_ID 9",
                        "RA 0.0",
                        "DEC 0.0",
                        "BEAM 0"
                    ]
                }
            }
        ]
    }
    {% if not loop.last %},{% endif %}
    {% endfor %}

    ]
}